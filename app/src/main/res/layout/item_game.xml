<?xml version="1.0" encoding="utf-8"?>
<!-- თამაშის ბარათის განლაგება — RecyclerView-ის ელემენტი -->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/cardView"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="8dp"
    android:layout_marginEnd="8dp"
    android:layout_marginTop="4dp"
    android:layout_marginBottom="4dp"
    app:cardBackgroundColor="@color/retro_card"
    app:cardCornerRadius="4dp"
    app:strokeColor="@color/retro_card_stroke"
    app:strokeWidth="2dp"
    app:cardElevation="0dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <!-- გადაადგილების ინდიკატორი (≡) — drag-and-drop-ისთვის -->
        <ImageView
            android:id="@+id/imageViewDragHandle"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_drag_handle"
            android:contentDescription="@string/drag_hint"
            app:tint="@color/retro_text_secondary"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <!-- თამაშის სათაური -->
        <TextView
            android:id="@+id/textViewTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textColor="@color/retro_green"
            android:textSize="18sp"
            android:textStyle="bold"
            android:fontFamily="monospace"
            android:layout_marginStart="12dp"
            android:ellipsize="end"
            android:maxLines="1"
            tools:text="Super Mario Bros"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@id/imageViewDragHandle"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- პლატფორმა -->
        <TextView
            android:id="@+id/textViewPlatform"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textColor="@color/retro_cyan"
            android:textSize="14sp"
            android:fontFamily="monospace"
            android:layout_marginStart="12dp"
            android:layout_marginTop="2dp"
            tools:text="NES"
            app:layout_constraintTop_toBottomOf="@id/textViewTitle"
            app:layout_constraintStart_toEndOf="@id/imageViewDragHandle"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- სტატუსი — Backlog / Playing / Finished -->
        <TextView
            android:id="@+id/textViewStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/retro_yellow"
            android:textSize="12sp"
            android:fontFamily="monospace"
            android:textAllCaps="true"
            android:layout_marginStart="12dp"
            android:layout_marginTop="4dp"
            tools:text="PLAYING"
            app:layout_constraintTop_toBottomOf="@id/textViewPlatform"
            app:layout_constraintStart_toEndOf="@id/imageViewDragHandle" />

        <!-- ვარსკვლავებით რეიტინგის ჩვენება -->
        <LinearLayout
            android:id="@+id/ratingContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginStart="12dp"
            android:layout_marginTop="4dp"
            app:layout_constraintTop_toBottomOf="@id/textViewStatus"
            app:layout_constraintStart_toEndOf="@id/imageViewDragHandle">

            <ImageView
                android:id="@+id/star1"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@drawable/ic_star_empty"
                android:contentDescription="Star 1" />

            <ImageView
                android:id="@+id/star2"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_marginStart="2dp"
                android:src="@drawable/ic_star_empty"
                android:contentDescription="Star 2" />

            <ImageView
                android:id="@+id/star3"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_marginStart="2dp"
                android:src="@drawable/ic_star_empty"
                android:contentDescription="Star 3" />

            <ImageView
                android:id="@+id/star4"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_marginStart="2dp"
                android:src="@drawable/ic_star_empty"
                android:contentDescription="Star 4" />

            <ImageView
                android:id="@+id/star5"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_marginStart="2dp"
                android:src="@drawable/ic_star_empty"
                android:contentDescription="Star 5" />

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
